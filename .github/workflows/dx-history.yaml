name: Trigger Labeler

on:
  workflow_dispatch:
    inputs:
      pr_number:
        required: false
        type: string
        default: ''

jobs:
  call-dx-labeler:
    uses: stone-payments/github-runner-validator/.github/workflows/dx-labeler.yaml@505a2db0c83bf410384f357c98aa4646a9df1e20 # main
    with:
      pr_number: ${{ inputs.pr_number }}
    secrets: inherit
